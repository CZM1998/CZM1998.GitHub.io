<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>C语言基本语法速览</title>
    <link href="/20201110-C%E8%AF%AD%E8%A8%80%E5%9F%BA%E6%9C%AC%E8%AF%AD%E6%B3%95%E9%80%9F%E8%A7%88/"/>
    <url>/20201110-C%E8%AF%AD%E8%A8%80%E5%9F%BA%E6%9C%AC%E8%AF%AD%E6%B3%95%E9%80%9F%E8%A7%88/</url>
    
    <content type="html"><![CDATA[<h2 id="C的诞生与影响"><a href="#C的诞生与影响" class="headerlink" title="C的诞生与影响"></a>C的诞生与影响</h2><p>C语言诞生于1969年至1973年间，由丹尼斯·里奇与肯·汤普逊在B语言的基础上改造而成。C语言以其简洁、高效、灵活的语法和极强的性能迅速流行起来。大量的基础性软件由C语言制作，成为现在许多服务的基础。日至今日，C语言仍然在编程语言中占据极为重要的地位，是目前最流行的语言之一。而在C语言诞生之后的语言，也或多或少的受到C语言的影响。诸如C++，Java，C#等语言中，都能看到C语言的影子。</p><h2 id="一段包含基本语法的C程序"><a href="#一段包含基本语法的C程序" class="headerlink" title="一段包含基本语法的C程序"></a>一段包含基本语法的C程序</h2><p>C语言语法简单，结构清晰，学习容易。你可以通过一个简单的程序，了解到C语言的大部分语法知识。并能够运行一个简易的C程序。</p><p>以下程序中包含C语言的一些基本语法，更多更详细的语法可以查阅C标准文件。</p><pre><code class="hljs c"><span class="hljs-comment">//  单行注释到行尾结束</span><span class="hljs-comment">/*</span><span class="hljs-comment">    多行注释可以任意放置但要注意嵌套问题</span><span class="hljs-comment">    注释是用来说明解释程序的，他非常重要</span><span class="hljs-comment">*/</span><span class="hljs-comment">// 引入头文件便能使用头文件声明的函数</span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&quot;my.h&quot;</span>    <span class="hljs-comment">// 引入自己的头文件</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdio.h&gt; // 引入标准库头文件</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdlib.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;string.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;math.h&gt;</span></span><span class="hljs-comment">// 使用宏定义一些直接替换文本的值</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> MAX_WIDTH 22</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> MAX_RADIUS 11</span><span class="hljs-comment">// 宏还可以进行判断，按照条件进行不同的编译</span><span class="hljs-meta">#<span class="hljs-meta-keyword">ifdef</span> __unix__</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYSTEM <span class="hljs-meta-string">&quot;unix&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">elif</span> __linux__</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYSTEM <span class="hljs-meta-string">&quot;linux&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">elif</span> _WIN32</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYSTEM <span class="hljs-meta-string">&quot;windows&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">elif</span> _WIN64</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SYSTEM <span class="hljs-meta-string">&quot;windows&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">else</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">error</span> <span class="hljs-meta-string">&quot;unkonw system&quot;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span><span class="hljs-comment">// 宏还可以定义函数，会将参数原样替换到后面的语句，多个语句用\换行</span><span class="hljs-comment">// sizeof 是C关键字，用于计算变量的大小</span><span class="hljs-comment">// C语言将 (类型) 放在变量前进行类型强制转换</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> MALLOC(type, name, len)                     \</span>    type* name = (type*)<span class="hljs-built_in">malloc</span>(<span class="hljs-keyword">sizeof</span>(type) * len); \    name = <span class="hljs-built_in">memset</span>(name, <span class="hljs-number">0</span>, <span class="hljs-keyword">sizeof</span>(type) * len)      \<span class="hljs-comment">// 宏里可以嵌套另一个宏，但嵌套自己的结果由编译器决定</span>#define CREATE_CIRCLE(type, name, len)                          \    MALLOC(type, name, len);                                    \    name-&gt;radius = <span class="hljs-number">0</span>;                                           \    name-&gt;width = <span class="hljs-number">0</span>;                                            \    name-&gt;printCircle = print##type;                            \    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;type &quot;</span> #type <span class="hljs-string">&quot; has been created in %s\n&quot;</span>, SYSTEM)   \    <span class="hljs-comment">//在宏里可以使用两个#号拼接一个标识符</span>    <span class="hljs-comment">//使用一个#将其转化成字符常量</span><span class="hljs-comment">// 结构体可以将不同的数组组成一个数据类型，甚至函数指针</span><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Position</span> &#123;</span>    <span class="hljs-keyword">int</span> x;    <span class="hljs-keyword">int</span> y;&#125;;<span class="hljs-comment">// typedef 可以用另一个名字来代替当前类型</span><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> _<span class="hljs-title">Circle</span> &#123;</span>    <span class="hljs-keyword">int</span> radius;    <span class="hljs-keyword">int</span> width;    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Position</span> <span class="hljs-title">point</span>;</span>    <span class="hljs-comment">// 函数指针可以指向一个同形式的函数并像正常函数一样调用</span>    <span class="hljs-comment">// 在结构体中声明自己类型的变量，</span>    <span class="hljs-keyword">void</span> (*printCircle)(struct _Circle*);&#125; Circle;<span class="hljs-comment">// 函数先声明再使用</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">printCircle</span><span class="hljs-params">(Circle* circle)</span> </span>&#123;    <span class="hljs-comment">// 使用&amp;获取变量的地址</span>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Position</span>* <span class="hljs-title">position</span> = &amp;<span class="hljs-title">circle</span>-&gt;<span class="hljs-title">point</span>;</span>    <span class="hljs-comment">// 即使在同一个语句中，你可以声明多个变量，并为其单独赋值</span>    <span class="hljs-keyword">int</span> w, h = <span class="hljs-number">0</span>;    <span class="hljs-comment">// 你甚至可以像这样从右往左依次赋同一个值</span>    <span class="hljs-keyword">int</span> i = w = <span class="hljs-number">0</span>;    <span class="hljs-keyword">do</span> &#123;        <span class="hljs-comment">// C语言中，0表示假，非0表示真。所有表达式的结果都会成为0或非0</span>        <span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>) &#123;            <span class="hljs-comment">// 变量加一，有四种写法</span>            <span class="hljs-keyword">for</span>(h = <span class="hljs-number">0</span>; h &lt; circle-&gt;width; h += <span class="hljs-number">1</span>) &#123;                <span class="hljs-keyword">for</span>(w = <span class="hljs-number">0</span>; w &lt; circle-&gt;width; w = w + <span class="hljs-number">1</span>) &#123;                    <span class="hljs-keyword">int</span> distance = <span class="hljs-built_in">pow</span>(h - position-&gt;x, <span class="hljs-number">2</span>) + <span class="hljs-built_in">pow</span>(w - position-&gt;y, <span class="hljs-number">2</span>);                    <span class="hljs-keyword">int</span> r = <span class="hljs-built_in">pow</span>(circle-&gt;radius, <span class="hljs-number">2</span>);                    <span class="hljs-comment">// 对于一些二选一的判断，可以使用三目运算</span>                    <span class="hljs-keyword">char</span> result = distance &lt;= <span class="hljs-built_in">pow</span>(circle-&gt;radius, <span class="hljs-number">2</span>) ? <span class="hljs-string">&#x27;*&#x27;</span> : <span class="hljs-string">&#x27; &#x27;</span>;                    <span class="hljs-comment">// 结果繁多时，可以使用switch选择某种已知结果操作，如果没有加上break，会一直执行下去</span>                    <span class="hljs-keyword">switch</span> (result) &#123;                        <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;*&#x27;</span>: <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%c&quot;</span>,result); <span class="hljs-keyword">break</span>;                        <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27; &#x27;</span>:                        <span class="hljs-keyword">default</span>: <span class="hljs-comment">// 所有结果不匹配时会执行此处语句</span>                            <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%c&quot;</span>,<span class="hljs-string">&#x27; &#x27;</span>); <span class="hljs-keyword">break</span>;                    &#125;                &#125;                <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;\n&quot;</span>);            &#125;            <span class="hljs-comment">// C语言的条件表达式中，你可以使用逻辑运算来组成复杂的判断</span>            <span class="hljs-comment">// 如 与&amp;&amp; 或|| 非! 等== 不等!= 小于 &lt; 大于&gt; 小于等于 &lt;= 大于等于 &gt;=</span>            <span class="hljs-keyword">if</span> (w != circle-&gt;width &amp;&amp; h != circle-&gt;width) &#123;                <span class="hljs-keyword">continue</span>; <span class="hljs-comment">// 继续吧！这个语句便是让循环跳过后面的语句，直接进入下一个循环中</span>            &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(w == circle-&gt;width &amp;&amp; h == circle-&gt;width) &#123;                <span class="hljs-comment">// 看上面，它是对另一种情况进行判断</span>                <span class="hljs-keyword">break</span>;            &#125;        &#125;        <span class="hljs-keyword">break</span>;    &#125; <span class="hljs-keyword">while</span> (++i); <span class="hljs-comment">// 这是C语言中的一种循环，他保证循环至少执行一次</span>&#125;<span class="hljs-comment">// 函数定义可以和声明分开，但声明一定要在main之前</span><span class="hljs-function">Circle* <span class="hljs-title">createCircle</span><span class="hljs-params">(<span class="hljs-keyword">int</span> x, <span class="hljs-keyword">int</span> y, <span class="hljs-keyword">int</span> radius, <span class="hljs-keyword">int</span> width)</span></span>;<span class="hljs-comment">// 一切C程序均从main函数执行</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">int</span> argc, <span class="hljs-keyword">char</span>** argv)</span> </span>&#123;    <span class="hljs-keyword">int</span> width[<span class="hljs-number">2</span>] = &#123;<span class="hljs-number">22</span>, <span class="hljs-number">18</span>&#125;; <span class="hljs-comment">// 数组采用此种方式声明并赋值初始化，如果初始化，则[]中的数字是不必要的</span>    <span class="hljs-keyword">int</span>* w = width; <span class="hljs-comment">// 数组名等价于一个指针</span>    <span class="hljs-keyword">int</span>* radius = (<span class="hljs-keyword">int</span>*)<span class="hljs-built_in">malloc</span>(<span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">int</span>) * <span class="hljs-number">2</span>); <span class="hljs-comment">// 动态分配大小的数组由此产生</span>    *(radius + <span class="hljs-number">1</span>) = <span class="hljs-number">8</span>;  <span class="hljs-comment">// 数组并不只能通过[]来访问，指针加偏移量也是可以的</span>    radius[<span class="hljs-number">0</span>] = <span class="hljs-number">10</span>;     <span class="hljs-comment">// 同样的，也可以用[]来访问动态大小的数组元素</span>    Circle* circle; <span class="hljs-comment">// 声明变量可以不初始化，但这样比较危险</span>    <span class="hljs-comment">// 循环的一种形式，由仅执行一次的初始语句，每次循环开始前执行的判断语句，每次循环结束后执行的语句组成</span>    <span class="hljs-comment">// i++ 表i执行后自增1 ++i表执行前自增1 其他符号同理</span>    <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">2</span>; i++) &#123;        <span class="hljs-comment">// 一般调用函数是这样的</span>        circle = createCircle( w[i] / <span class="hljs-number">2</span>, *(w + i) / <span class="hljs-number">2</span>, radius[i], w[i]);        <span class="hljs-keyword">void</span> (*print)(Circle*) = circle-&gt;printCircle;        <span class="hljs-comment">// 当然也可以是这样的</span>        (*print)(circle);        <span class="hljs-comment">// 动态申请的内存需要自己手动释放，避免出现内存泄露</span>        <span class="hljs-built_in">free</span>(circle);    &#125;    <span class="hljs-comment">// 非void函数需要返回值</span>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;<span class="hljs-function">Circle* <span class="hljs-title">createCircle</span><span class="hljs-params">(<span class="hljs-keyword">int</span> x, <span class="hljs-keyword">int</span> y, <span class="hljs-keyword">int</span> radius,<span class="hljs-keyword">int</span> width)</span> </span>&#123;    CREATE_CIRCLE(Circle, circle, <span class="hljs-number">1</span>);    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Position</span> <span class="hljs-title">position</span>;</span>    position.x = x;    position.y = y;             <span class="hljs-comment">// 在栈上申请的内存可以直接访问</span>    circle-&gt;point = position;   <span class="hljs-comment">// 在堆上申请的内存需要解引用才能访问</span>    (*circle).radius = radius;  <span class="hljs-comment">// 对结构体解引用有两种写法，-&gt;是这种写法的简化</span>    circle-&gt;width = width;    <span class="hljs-keyword">return</span> circle;&#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>C语言</category>
      
    </categories>
    
    
    <tags>
      
      <tag>学海拾贝</tag>
      
      <tag>一得之见</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>软件工程间接性原理</title>
    <link href="/20200907-%E9%97%B4%E6%8E%A5%E6%80%A7%E5%8E%9F%E7%90%86/"/>
    <url>/20200907-%E9%97%B4%E6%8E%A5%E6%80%A7%E5%8E%9F%E7%90%86/</url>
    
    <content type="html"><![CDATA[<h1 id="间接性原理"><a href="#间接性原理" class="headerlink" title="间接性原理"></a>间接性原理</h1><p>计算机科学领域的任何问题都可以通过增加一个间接的中间层来解决。</p><p>“All problems in computer science can be solved by another level of indirection”是David John Wheeler的著名理论，这一理论在涉及计算机的方方面面都发挥着巨大的威力。</p><p><del>所以其实是计算机技术与套娃工程，软件工程与套娃咯</del></p><iframe src="https://czm.cool/" width="100%" height="500" name="matryoshka" scrolling="yes" noresize="noresize" frameborder="0" id="matryoshka">]]></content>
    
    
    <categories>
      
      <category>其他</category>
      
    </categories>
    
    
    <tags>
      
      <tag>自娱自乐</tag>
      
      <tag>只言片语</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
